PySide6>=6.6
Pillow>=10.0
openpyxl>=3.1

# --- опционально (подключаются по этапам) ---
# requests>=2.31        # онлайн-поиск изображений (Этап 7)
# cairosvg>=2.7         # SVG -> PNG, экспорт SVG (Этап 6)
# rembg>=2.0            # замена фона нейросетью (Этап 3+)
